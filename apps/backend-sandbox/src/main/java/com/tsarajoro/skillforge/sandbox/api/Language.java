package com.tsarajoro.skillforge.sandbox.api;

/**
 * Langages supportes par la sandbox en V1.
 *
 * Chaque langage est mappe a une image Docker (cf. application.yml > runtimes).
 */
public enum Language {
    PHP,
    JS
}
